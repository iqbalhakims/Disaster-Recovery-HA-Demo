apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "salesme-backend.fullname" . }}
  labels:
    {{- include "salesme-backend.labels" . | nindent 4 }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      {{- include "salesme-backend.selectorLabels" . | nindent 6 }}
