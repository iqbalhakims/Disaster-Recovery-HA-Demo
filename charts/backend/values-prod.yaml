replicaCount: 2

image:
  tag: "latest"

ingress:
  enabled: true
  hosts:
    - host: api.iqbalhakim.ink
      paths:
        - path: /
          pathType: Prefix

autoscaling:
  enabled: true
  minReplicas: 4
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
